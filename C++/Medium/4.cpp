#include <iostream>
using namespace std;
int main()
{
    int x = 10;
    while (x != 0)
    {
        cout << "Looping..." << endl;
        x--;
    }
    return 0;

    // not sure the purpose but I think you guys wanted to loop till  becomes 0 from 10
    // so I just .. sorry the x button on my keyboard is not working did from == to != to backward
}
